import { UrlValueNumberPipe } from './url-value-number.pipe';

describe('UrlValueNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlValueNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
